{
  "name": "ntt",
  "version": "1.0.0",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "bun run --filter '@wormhole-foundation/sdk-definitions-ntt' build && bun run --filter '*' build",
    "rebuild": "bun run --filter '*' rebuild",
    "build:solana": "bun run --filter '@wormhole-foundation/sdk-definitions-ntt' build && bun run --filter '@wormhole-foundation/sdk-solana-ntt' build",
    "build:evm": "bun run --filter '@wormhole-foundation/sdk-definitions-ntt' build && bun run --filter '@wormhole-foundation/sdk-evm-ntt' build",
    "build:sui": "bun run --filter '@wormhole-foundation/sdk-definitions-ntt' build && bun run --filter '@wormhole-foundation/sdk-sui-ntt' build",
    "typecheck:cli": "bun run --filter '@wormhole-foundation/ntt-cli' typecheck",
    "generate": "bun run --filter '*' generate",
    "test": "bun --workspaces --if-present run test",
    "test:ci": "CI=true NODE_OPTIONS=--experimental-vm-modules jest --config ./jest.config.ts",
    "test:local": "NODE_OPTIONS=--experimental-vm-modules jest --config ./jest.config.ts",
    "generate:test": "bun run --filter '*' generate:test",
    "build:deps": "bun run --filter '*' build:contracts && bun run generate",
    "prettier": "bun run --filter '*' prettier",
    "prettier:check": "prettier --check .",
    "version": "tsx setSdkVersion.ts"
  },
  "devDependencies": {
    "@mysten/sui": "^2.17.0",
    "@solana/spl-token": "0.3.9",
    "@solana/spl-token-metadata": "^0.1.6",
    "@solana/web3.js": "^1.95.8",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.2",
    "@wormhole-foundation/sdk": "6.1.4",
    "@wormhole-foundation/wormchain-sdk": "^0.0.1",
    "ethers": "^6.5.1",
    "prettier": "3.6.2",
    "ts-jest": "^29.1.2",
    "tsx": "^4.7.2",
    "typescript": "^5.1.1"
  },
  "workspaces": [
    "sdk/definitions",
    "evm/ts",
    "solana",
    "sui/ts",
    "xrpl/ts",
    "sdk/route",
    "sdk/examples",
    "cli"
  ],
  "overrides": {
    "@types/jest": "^29.5.12",
    "@wormhole-foundation/sdk-sui": "6.1.4",
    "@wormhole-foundation/sdk-sui-core": "6.1.4",
    "@wormhole-foundation/sdk-base": "file:./vendor/wormhole-foundation-sdk-base-6.1.4-canton.tgz"
  },
  "dependencies": {
    "@wormhole-foundation/sdk-sui": "6.1.4",
    "@wormhole-foundation/sdk-sui-core": "6.1.4"
  }
}
