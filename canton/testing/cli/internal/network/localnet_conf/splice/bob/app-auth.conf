# Wormhole addition: mirrors conf/splice/app-user/app-auth.conf's unsafe HS256 auth block.
canton.validator-apps.bob-validator_backend {
  participant-client.ledger-api.auth-config = {
    type = "self-signed"
    user = ledger-api-user
    audience = "https://canton.network.global"
    secret = "unsafe"
  }

  auth = {
    algorithm = "hs-256-unsafe"
    audience = "https://canton.network.global"
    secret = "unsafe"
  }

  ledger-api-user = ledger-api-user
  validator-wallet-users.0 = ledger-api-user
}
